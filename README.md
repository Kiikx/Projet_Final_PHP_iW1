# Projet Final PHP iW1

## Lancement du projet

Pour lancer le projet, il suffit d'exécuter la commande suivante :

```sh
docker-compose up
```

Cette commande va démarrer tous les services définis dans le fichier `docker-compose.yml`.